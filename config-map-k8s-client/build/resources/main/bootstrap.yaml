logging:
  level:
    org:
      springframework: DEBUG

spring:
  cloud:
    kubernetes:
      config:
        namespace: spring-k8s
        sources:
          - name: config-map-multiple-entries-single-yaml